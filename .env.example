# Paths to working directories.
# If a variable is not set, the app falls back to the matching subfolder
# in the project root (templates/, broken/, fixed/).

# TEMPLATES_DIR=/absolute/path/to/templates
# BROKEN_DIR=/absolute/path/to/broken
# FIXED_DIR=/absolute/path/to/fixed
