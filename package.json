{
  "name": "trackfix",
  "version": "1.1.0",
  "description": "Snap a broken GPX track onto a reference template polyline",
  "scripts": {
    "fix": "ts-node app.ts",
    "build": "tsc",
    "build:exe": "bash build-exe.sh"
  },
  "dependencies": {
    "fast-xml-parser": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "esbuild": "^0.25.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
